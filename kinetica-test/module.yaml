product:
  type: kmp/lib
  platforms: [jvm, android, js, wasmJs, macosArm64]

apply:
  - ../common.module-template.yaml

dependencies:
  - ../kinetica-runtime: exported

description: "Headless test runtime for rendering, semantic queries, events, virtual time, and journals."

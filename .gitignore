# Reference checkouts (separate .git, huge — not part of this project)
/projects/

# Build outputs and toolchain caches
build/
**/build/
.kotlin/

# Project Worktrees
.worktrees/

# Local tooling (vendored per machine, provisioned in CI)
.tools/
.playwright-browsers/
.npm-cache/

# Release credentials: the wrapper that feeds signing key and Central token to
# scripts/release.sh. Never commit it.
/publish.sh

# IDE / OS
.idea/
.DS_Store

# JS
**/node_modules/
*.log

# Generated / staging
/kinetica-gtk/cinterop/gtk4.def
/bench/dist/
/bench/results/dom-count-*.json
/bench/results/profile-*/
/bench/results/cpuprofiles/
/bench/results/runs/
/docs/.docker-stage/
.claude/
.zcode/

product:
  type: kmp/lib
  platforms: [jvm, android, js, wasmJs, macosArm64]

apply:
  - ../common.module-template.yaml

dependencies:
  - ../kinetica-runtime: exported

description: "First-party motion primitives over onExit and frameValue."

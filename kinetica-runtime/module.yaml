product:
  type: kmp/lib
  platforms: [jvm, android, js, wasmJs, macosArm64, linuxX64]

apply:
  - ../common.module-template.yaml

settings:
  kotlin:
    optIns:
      - kotlin.concurrent.atomics.ExperimentalAtomicApi

description: "Core Kinetica runtime: Node values, cells, effects, resources, navigation, and renderer contracts."

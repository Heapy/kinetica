product:
  type: kmp/lib
  platforms: [jvm, android, js, wasmJs, macosArm64]

apply:
  - ../common.module-template.yaml

dependencies:
  - ../kinetica-runtime: exported

description: "First-party theme context helpers, design tokens, breakpoints, and layout direction."
